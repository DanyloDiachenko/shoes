import { Cart } from '@/components/cart/cart';

export const metadata = {
	title: 'Sneakers shop | Cart',
	description: 'Sneakers shop cart page.'
};

const CartPage = () => {
	return <Cart />;
};

export default CartPage;
