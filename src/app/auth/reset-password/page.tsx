export const metadata = {
	title: 'Reset my password | sneakers shop',
	description: 'Sneakers shop reset password page.'
};

const ResetPage = () => {
	return <>reset password</>;
};

export default ResetPage;
